# 로보독 화재 대응 프로젝트 — 통합 작업 핸드오프 문서

이 문서는 지금까지 확정한 설계를 정리한 것입니다. 클로드코드에서 이 문서를 보여주고 "이 문서대로 통합해줘"라고 요청하면 이어서 작업할 수 있습니다.

---

## 1. 확정된 최종 아키텍처

```
[로보독 몸체]  ---무선 USB 동글---→  [PC (고정, Flask 서버)]
[ESP32 센서]   ---Wi-Fi(HTTP)------→  [PC (고정, Flask 서버)]
[폰 카메라]    ---Wi-Fi(IP웹캠)----→  [PC (고정, Flask 서버)]
                                          │
                                          ├─ 전역 변수(state)에 저장
                                          ├─ 판단 로직 (화재/가스/통로폭)
                                          ├─ 로보독 제어 (dog.move / dog.gesture / dog.sound)
                                          └─ /status, /snapshot.jpg 제공
                                                    │
                                          ---Wi-Fi(폴링)---→  [앱인벤터 대시보드]
```

**핵심 포인트**
- PC는 책상에 고정, 로보독/ESP32/폰카메라는 전부 무선으로 PC와 통신 → 로보독은 자유롭게 이동 가능
- Firebase 없이 파이썬 프로세스 내 전역 변수가 "DB" 역할
- 카메라 1대(폰)로 OCR(번호판)과 화재 현장 사진 캡처를 동시에 처리
- 119 신고는 실제 자동 신고 불가 → 시뮬레이션(알림 화면/로그)으로 대체

---

## 2. 보유 파일 및 역할

### 2-1. 기존에 업로드한 파일 (`tools` 패키지 — 로보독 자체 제어 라이브러리)

| 파일 | 역할 |
|---|---|
| `tools/__init__.py` | 패키지 초기화, `deflib`/`robodog` 심볼 export |
| `tools/robodog.py` | `RoboDog` 클래스 — 무선 USB로 로보독과 시리얼 통신, `move()`/`gesture()`/`sound()`/`headLEDExp()` 등 제공 |
| `tools/deflib.py` | 상수 정의 (동작 코드, 색상, 사운드 트랙 번호 등) + 패킷 체크섬 유틸 |
| `tools/packet.py` | 로보독에 보낼 명령 패킷 생성 |
| `tools/parse.py` | 로보독에서 받은 응답 패킷 파싱 |
| `tools/dance_lib.py` | (내용 미확인 — 댄스/제스처 관련 추정) |
| `main.py` (기존 예제) | ESP32를 **시리얼(COM3)**로 읽어서 장애물 감지 시 로보독 정지시키는 테스트 코드 — 이제 ESP32는 Wi-Fi로 바뀌므로 이 파일의 시리얼 읽기 부분은 더 이상 안 씀 |

### 2-2. 이전에 만든 파일

| 파일 | 역할 |
|---|---|
| `robodog_server.py` | Flask 중앙 서버 — 센서값 목업, 카메라 캡처, `/status`, `/video_feed`, `/snapshot.jpg` 제공 |
| `로보독_화재대응_개발가이드.md` | 전체 개발 로드맵 문서 |

### 2-3. 아직 없는 것 (새로 작성 필요)

- ESP32 아두이노 스케치 (Wi-Fi로 센서값을 Flask 서버에 POST하는 코드)
- `main.py`와 `robodog_server.py`를 합친 **최종 통합 서버**
- 화재 감지 시 로보독 반응 로직 (정지 + 음성/제스처)
- TTS 또는 사운드 기반 대피 안내 로직
- 번호판 OCR 모듈
- 온도 추세(dT/dt) 계산 로직

---

## 3. 통합 작업 목록 (클로드코드에게 순서대로 요청할 작업)

### 작업 1 — 프로젝트 폴더 구조 정리
```
robodog-project/
├── tools/                      # 기존 로보독 제어 라이브러리 (그대로 사용)
│   ├── __init__.py
│   ├── robodog.py
│   ├── deflib.py
│   ├── packet.py
│   ├── parse.py
│   └── dance_lib.py
├── server.py                   # main.py + robodog_server.py 통합본 (새로 작성)
├── esp32_firmware/
│   └── esp32_firmware.ino      # ESP32 아두이노 스케치 (새로 작성)
└── requirements.txt
```

### 작업 2 — ESP32 펌웨어를 시리얼 → Wi-Fi HTTP POST 방식으로 작성
- ESP32에서 불꽃/가스/온습도/초음파 센서값을 1초마다 읽어서
- PC의 Flask 서버 주소(`http://<PC IP>:5000/sensor_update`)로 JSON POST
- 예: `{"flame": 0, "gas": 320, "temp": 24.5, "dist_cm": 350}`

### 작업 3 — `server.py`에 `/sensor_update` 라우트 추가
- `robodog_server.py`의 목업 `sensor_worker()` 스레드는 제거하거나 개발용으로만 남김
- ESP32가 보낸 실제 데이터를 받아서 `update_state(...)` 호출하도록 변경

### 작업 4 — 로보독 제어 객체를 서버에 통합
- `server.py` 시작 시 `dog = RoboDog()` 생성, `dog.Open("COM포트")`로 연결 (무선 동글이 잡는 COM 포트)
- 로보독 초기 상태: 순찰 이동 (`dog.move(...)`)

### 작업 5 — 화재 감지 시 로보독 반응 로직 작성
- `state["fire_alert"] == True`가 되는 순간:
  1. `dog.move(STOP)` 또는 정지 제스처 호출
  2. 대피 안내 (사운드 트랙 재생 `dog.sound(...)` 또는 TTS로 mp3 생성 후 재생)
  3. 그 시점의 카메라 프레임을 `cv2.imwrite()`로 저장 → `state["photo_path"]`에 기록
  4. 119 알림 시뮬레이션 로그/플래그 세팅

### 작업 6 — 통로 폭 판단 로직
- 초음파 거리 값이 400cm 미만으로 **일정 시간(예: 2초) 이상 지속**될 때만 `narrow_passage = True`로 판정 (순간 노이즈 방지)

### 작업 7 — 온도 추세 판단 로직
- 최근 N개 온도값을 저장해두고 분당 상승률(dT/dt) 계산
- 임계값 초과 시 사전 경보 플래그

### 작업 8 — 폰 카메라(IP 웹캠) 연동
- `camera_worker()`의 `cv2.VideoCapture(0)`를
- `cv2.VideoCapture("http://<폰IP>:포트/video")`로 교체

### 작업 9 — 번호판 OCR 통합
- HelloAI `OCR()` 또는 EasyOCR 중 택1
- 매 프레임이 아니라 0.5~1초 간격으로만 실행 (성능 고려)
- 번호판 패턴 정규식으로 필터링 → `state["plate_number"]`

### 작업 10 — 앱인벤터 연동 확인
- `/status`, `/snapshot.jpg`는 기존 그대로 사용 가능 (변경 없음)
- Web+Clock+Image 블록 구성은 이전에 정리한 대로 진행

---

## 4. 클로드코드에 붙여넣을 요청 예시

```
아래 문서(로보독_통합_핸드오프.md)를 참고해서 진행해줘.

1. 먼저 작업 1(폴더 구조)부터 시작해줘
2. 그 다음 작업 3, 4, 5(서버 통합 + 로보독 화재 반응 로직)를 순서대로 해줘
3. ESP32 펌웨어(작업 2)는 별도로 요청할 때 진행해줘
4. tools 폴더 안 코드는 수정하지 말고 그대로 import해서 사용해줘
```

---

## 5. 주의사항 (클로드코드 작업 시 참고)

- `tools/robodog.py`는 이미 완성된 라이브러리이므로 **수정하지 말고 그대로 import**해서 씁니다.
- 로보독 무선 동글은 실내 기준 대략 10~30m 범위 — 발표 시나리오 범위 내에서는 문제없음.
- `dog.sound()`는 TTS가 아니라 **미리 저장된 트랙 재생** 방식이라, 자유 문장 음성 안내가 필요하면 `gTTS`/`pyttsx3`로 mp3를 만들어 별도 재생하는 방식을 추가해야 함.
- ESP32와 PC가 같은 Wi-Fi에 있어야 함 (앱인벤터 폰도 마찬가지).
- 119 자동 신고는 실제로 구현하지 않고 시뮬레이션(화면 알림/로그)으로 처리.
