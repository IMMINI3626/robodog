# 센서 회로 연결

## 전원 레일

| 레일 | 연결 |
|---|---|
| + (5V) | ESP32 `5V`(`VIN`) 핀 → 브레드보드 + 레일 |
| − (GND) | ESP32 `GND` 핀 → 브레드보드 − 레일 |

모든 센서의 VCC/GND는 이 레일에 연결. 신호선만 개별 GPIO로 연결.

## 센서 → ESP32 GPIO

| 센서 | VCC | GND | 신호 | GPIO |
|---|---|---|---|---|
| 가스 센서 (MQ) | + 레일 | − 레일 | AO | GPIO 34 |
| 불꽃 센서 | + 레일 | − 레일 | DO | GPIO 27 |
| DHT11 온습도 | + 레일 | − 레일 | S | GPIO 14 |
| 초음파 좌 (HC-SR04) | + 레일 | − 레일 | TRIG | GPIO 5 |
| | | | ECHO | GPIO 18 (분압 회로 경유) |
| 초음파 우 (HC-SR04) | + 레일 | − 레일 | TRIG | GPIO 19 |
| | | | ECHO | GPIO 23 (분압 회로 경유) |

GPIO 34는 ADC1 입력전용 핀 (Wi-Fi 사용 중에도 아날로그 읽기 안정). 나머지는 디지털 읽기라 핀 제약 없음.

## 초음파 ECHO 전압분배 (양쪽 각각 필요)

HC-SR04 ECHO 출력은 5V, ESP32 입력 허용은 3.3V. 저항 2개로 절반(2.5V)으로 낮춤. 110Ω 저항 2개씩, 총 4개 사용.

```
좌측 ECHO ──[110Ω]──●──[110Ω]── GND
                     │
                GPIO18 (약 2.5V)

우측 ECHO ──[110Ω]──●──[110Ω]── GND
                     │
                GPIO23 (약 2.5V)
```

TRIG은 분압 없이 GPIO에 직결.

## DHT11 풀업

모듈에 풀업 저항 내장된 경우가 대부분 → VCC/GND/S 3선만 연결. 값이 `nan`으로만 나오면 VCC-S 사이 4.7~10kΩ 저항 추가.

## 조립 순서

1. ESP32에 +/− 레일만 먼저 연결
2. 센서 하나씩 연결 → 시리얼 모니터로 값 확인 → 다음 센서 진행
   - 순서: DHT11 → 불꽃 → 가스 → 초음파(좌) → 초음파(우)
3. 전체 확인 후 브레드보드+ESP32를 로보독 몸체에 물리적으로 고정 (전기적 연결 아님, 테이프/케이블타이/벨크로로 얹기만)
4. 전원을 PC USB → 보조배터리(5V, 5000mAh 이상)로 교체

## 주의사항

- 가스/초음파 VCC는 5V 필수 (3.3V 레일 금지)
- ECHO는 분압 저항 없이 GPIO 직결 금지 (ESP32 손상)
- 각 모듈 VCC/GND 극성은 실크스크린 라벨로 연결 직전 재확인
- 초음파 좌/우는 로보독 정면 기준 좌우로 벌려서 부착 (각 45도 정도)
- 로보독과 ESP32는 전기적으로 연결되지 않음. 로보독은 무선동글↔PC, ESP32는 Wi-Fi↔PC로 각각 통신하고 PC(`server.py`)가 중계
